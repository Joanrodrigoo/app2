
// Nota: Este archivo es de solo lectura, así que no lo podemos modificar directamente.
// En una situación real, modificaríamos el componente para incluir una función callback
// para notificar cuando el registro se ha completado.

// Dado que no podemos modificar el archivo, debemos adaptarnos en RegisterPage.tsx
// para simular el comportamiento deseado.
